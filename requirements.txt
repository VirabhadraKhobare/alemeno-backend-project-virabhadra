streamlit==1.35.0
openai==0.28.0
python-dotenv==1.0.0
requests==2.31.0
openai==0.28.0
python-dotenv==1.0.0
requests==2.31.0

# Notes: this requirements.txt is kept minimal for Streamlit Cloud to avoid
# installing native/development-only packages (psycopg2, gunicorn, test tools).
# Use `backend-requirements.txt` to install the full backend dependencies.